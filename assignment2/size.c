#include<stdio.h>
#include<conio.h>
int main()
{ int a;
char b;
float c;
double d;
printf(" size of int is %d\n",sizeof(a));
printf(" size of char is%d\n",sizeof(b));
printf("size of float is %d\n",sizeof(c));
printf(" size of double is %d\n",sizeof(d));

    getch();
    return 0;
}
